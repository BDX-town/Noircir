{
  "name": "eleventy",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "template": "^0.0.0"
  },
  "scripts": {
    "start": "npx @11ty/eleventy --config=.eleventy.prod.js --watch",
    "test": "npx @11ty/eleventy --config=.eleventy.common.js --serve"
  }
}
